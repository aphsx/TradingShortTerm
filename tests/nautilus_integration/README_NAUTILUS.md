# NautilusTrader Integration for VORTEX-7 🧠

โฟลเดอร์นี้รวบรวมไฟล์สำหรับการทำ Backtest และเชื่อมต่อระบบ VORTEX-7 เข้ากับ **NautilusTrader Framework** เพื่อประสิทธิภาพและความแม่นยำสูงสุด

## 📁 โครงสร้างไฟล์
*   `strat_adapter.py`: **(หัวใจหลัก)** ทำหน้าที่เป็น Adapter แปลง Event จาก Nautilus (Bar, Tick, Book) ให้เข้ากับ Engine E1-E5 ของเรา
*   `backtest_runner.py`: สคริปต์สำหรับรันการทดสอบ Backtest
*   `data_utils.py`: เครื่องมือช่วยแปลง Data Format ระหว่าง CCXT/Binance กับ Nautilus

## 🚀 วิธีการใช้งาน

### 1. การเตรียมข้อมูล
NautilusTrader ต้องการข้อมูลในรูปแบบ `Parquet` หรือ `CSV` ที่มีโครงสร้างเฉพาะ คุณสามารถใช้ `data_utils.py` ช่วยในการจัดการได้

### 2. การรัน Backtest
แก้ไขไฟล์ `backtest_runner.py` เพื่อระบุตำแหน่งไฟล์ข้อมูลประวัติของคุณ จากนั้นรัน:
```bash
python -m nautilus_integration.backtest_runner
```

### 3. ทำไมต้องใช้ไฟล์เหล่านี้แยกออกมา?
*   **Decoupling**: เพื่อให้โค้ดหลัก (`main.py`, `core.py`) ยังคงทำงานแบบ Live Trading ผ่าน CCXT ได้เหมือนเดิม
*   **Stability**: การแก้ไข Logic ใน Engine จะมีผลทั้งใน Backtest และ Live ทันที เพราะเราใช้ Engine เดียวกันผ่าน Adapter
*   **Scalability**: หากต้องการเปลี่ยนไปใช้ NautilusTrader สำหรับ Live Trading ในอนาคต คุณเพียงแค่เปลี่ยนจาก `BacktestEngine` เป็น `LiveEngine` ในสคริปต์ Runner

---
*VORTEX-7 - Alpha Integration Module*
