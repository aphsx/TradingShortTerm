{
  "permissions": {
    "allow": [
      "Bash(python run_node.py)",
      "Bash(python -c \"\nfrom nautilus_trader.backtest.node import BacktestNode\nfrom pathlib import Path\n\n# ดู attributes ทั้งหมดของ BacktestResult\nimport inspect\nfrom nautilus_trader.backtest.results import BacktestResult\n\nprint(''=== BacktestResult Attributes ==='')\nfor attr in dir(BacktestResult):\n    if not attr.startswith(''_''):\n        print(f''  - {attr}'')\n\")",
      "Bash(python -c \"\nfrom nautilus_trader.backtest.node import BacktestNode\nfrom nautilus_trader.backtest.results import BacktestResult\n\n# ดู attributes ทั้งหมด\nresult = BacktestResult.__dict__\nprint(''=== BacktestResult Methods/Properties ==='')\nimport inspect\nmembers = inspect.getmembers(BacktestResult)\nfor name, obj in members:\n    if not name.startswith(''_'') and (''stat'' in name.lower() or ''fee'' in name.lower() or ''commission'' in name.lower() or ''pnl'' in name.lower()):\n        print(f''  {name}: {type(obj).__name__}'')\n\")"
    ],
    "deny": [],
    "ask": []
  }
}
